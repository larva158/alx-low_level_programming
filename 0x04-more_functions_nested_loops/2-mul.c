#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 * mul -  multiplies two integers.
 *@a: number
 *@b: number
 * Return: Always 0.
 */

int mul(int a, int b)
{
	return (a * b);
	return (0);
}
