#include "main.h"
#include <stdio.h>

/**
 *swap_int - swaps the values of two integers
 *@a: pointer
 *@b: pointer
 * Return: Always 0.
 */

void swap_int(int *a, int *b)
{
	int c;
	*a = c;
	*b = *a;
	*b = c;
}
