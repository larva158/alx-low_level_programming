#include "main.h"
#include <stdio.h>

/**
 * add -  adds two integers and returns the result.
 * @j: print int j
 * @k: print int k
 * Return: Always 0.
 */

int add(int j, int k)
{
	return (j + k);
}
