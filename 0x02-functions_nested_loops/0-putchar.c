#include <main.h>
/**
 * _putchar - writes the character to stdout
 *
 * Return: On success
 * On error, -1 is returned, and errno is set appropriately.
 */
int main(void)
{
	write(1, "_putchar\n", 8);
	return (0);
}
